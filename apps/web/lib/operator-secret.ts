// Local re-export so web code imports the operator-secret gate via the @/ alias.
// The implementation lives in @jaguar/mcp and is shared with the MCP tools.
export { assertOperatorSecret } from "@jaguar/mcp";
